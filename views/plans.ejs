<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Plan - SportTracker</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: rgba(0, 0, 0, 0.8);
            color: #e2e8f0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .modal {
            background-color: #334155;
            padding: 2rem;
            border-radius: 1rem;
            max-width: 500px;
            width: 100%;
            position: relative;
        }

        .close-btn {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: none;
            border: none;
            color: #94a3b8;
            font-size: 1.5rem;
            cursor: pointer;
        }

        .close-btn:hover {
            color: #f1f5f9;
        }

        .plan-title {
            font-size: 1.75rem;
            font-weight: bold;
            margin-bottom: 1rem;
            color: #f1f5f9;
        }

        .difficulty-badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 1rem;
            font-size: 0.875rem;
            font-weight: 500;
            margin-bottom: 1.5rem;
        }

        .difficulty-easy { background-color: #10b981; color: white; }
        .difficulty-medium { background-color: #f59e0b; color: white; }
        .difficulty-hard { background-color: #ef4444; color: white; }

        .plan-description {
            color: #94a3b8;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .exercises-title {
            font-size: 1.25rem;
            margin-bottom: 1rem;
            color: #f1f5f9;
        }

        .exercise-list {
            list-style: none;
            margin-bottom: 2rem;
        }

        .exercise-item {
            display: flex;
            align-items: center;
            padding: 0.75rem 0;
            border-bottom: 1px solid #475569;
        }

        .exercise-item:last-child {
            border-bottom: none;
        }

        .exercise-bullet {
            width: 8px;
            height: 8px;
            background-color: #3b82f6;
            border-radius: 50%;
            margin-right: 1rem;
        }

        .save-btn {
            width: 100%;
            background-color: #3b82f6;
            color: white;
            border: none;
            padding: 1rem;
            border-radius: 0.5rem;
            font-size: 1rem;
            cursor: pointer;
        }

        .save-btn:hover {
            background-color: #2563eb;
        }
    </style>
</head>
<body>
    <div class="modal">
        <button class="close-btn" onclick="goBack()">&times;</button>
        
        <h2 class="plan-title">Beginner Full Body</h2>
        
        <span class="difficulty-badge difficulty-easy">Easy</span>
        
        <p class="plan-description">
            A great starting point for building foundational strength.
        </p>
        
        <h3 class="exercises-title">Exercises:</h3>
        <ul class="exercise-list">
            <li class="exercise-item">
                <div class="exercise-bullet"></div>
                <span>Squats: 3 sets of 10</span>
            </li>
            <li class="exercise-item">
                <div class="exercise-bullet"></div>
                <span>Push-ups: 3 sets of 8</span>
            </li>
            <li class="exercise-item">
                <div class="exercise-bullet"></div>
                <span>Plank: 3 sets of 30s</span>
            </li>
        </ul>
        
        <button class="save-btn" onclick="savePlan()">Save to My Plans</button>
    </div>

    <script>
        function goBack() {
            window.history.back();
        }

        function savePlan() {
            alert('Plan saved successfully!');
            window.history.back();
        }

        // Close with Escape key
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                goBack();
            }
        });

        // Close when clicking outside modal
        document.body.addEventListener('click', function(event) {
            if (event.target === document.body) {
                goBack();
            }
        });
    </script>
</body>
</html>